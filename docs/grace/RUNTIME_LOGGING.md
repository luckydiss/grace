# Runtime Logging

`GRACE` пишет runtime evidence как машинно-читаемый audit trail.

Минимум для каждой bounded execution:

- `mc`
- `fc`
- `ba`
- `belief`
- `fact`

Логи не заменяют process artifacts, а дополняют их.
