# Autonomy Failure Artifacts

При failure-path `GRACE` использует три основных артефакта:

- `failure-memory`
- `forced-context`
- `loop-guard`

Они нужны для управляемого remediation, а не для неограниченных повторных попыток.
