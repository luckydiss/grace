# GRACE ADR

This directory stores short architectural decision records for non-trivial framework decisions.

- `0001` captures the workflow lifecycle promotion to `ARCHIVED` and the legacy dry-run requirement.
- `0002` captures the runtime helper consolidation and CLI parser normalization.
